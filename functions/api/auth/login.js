import{json,verifyPassword,createSession,sessionCookie,cleanEmail}from'../../lib/auth.js';export async function onRequestPost(context){const{request,env}=context;if(!env.DB)return json({error:'Base de datos no configurada.'},500);let b;try{b=await request.json()}catch{return json({error:'Solicitud inválida.'},400)}const email=cleanEmail(b.email),password=String(b.password||'');const user=await env.DB.prepare('SELECT * FROM users WHERE email=? COLLATE NOCASE').bind(email).first();if(!user||!(await verifyPassword(password,user.password_salt,user.password_hash)))return json({error:'Correo o contraseña incorrectos.'},401);const session=await createSession(env,user.id);return json({ok:true,user:{id:user.id,first_name:user.first_name,last_name:user.last_name,email:user.email}},200,{'Set-Cookie':sessionCookie(session.rawToken,session.expires)})}
